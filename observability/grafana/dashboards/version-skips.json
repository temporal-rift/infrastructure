{
  "uid": "temporal-rift-version-skips",
  "title": "Version skips",
  "tags": ["temporal-rift", "kafka"],
  "timezone": "browser",
  "schemaVersion": 39,
  "time": { "from": "now-1h", "to": "now" },
  "refresh": "15s",
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Skip counts by service and reason",
      "description": "timeline_kafka_consumer_skips_total and read_kafka_consumer_skips_total, by reason (unknown_type / unsupported_version) and consumer. No data until each service exposes /actuator/prometheus -- see infrastructure#37, timeline-service#101, read-service#88. game-service does not yet emit a skip counter -- see game-service#200 and infrastructure#37's Cross-Repo Dependencies.",
      "gridPos": { "h": 12, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "victoriametrics" },
      "targets": [
        {
          "refId": "A",
          "expr": "timeline_kafka_consumer_skips_total",
          "legendFormat": "timeline-service / {{reason}} / {{consumer}}"
        },
        {
          "refId": "B",
          "expr": "read_kafka_consumer_skips_total",
          "legendFormat": "read-service / {{reason}} / {{consumer}}"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] }
    }
  ]
}
