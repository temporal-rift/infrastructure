{
  "title": "Version skips",
  "rows": [
    {
      "panels": [
        {
          "title": "Skip counts by service and reason",
          "description": "timeline_kafka_consumer_skips_total and read_kafka_consumer_skips_total, by reason (unknown_type / unsupported_version) and consumer.",
          "width": 12,
          "expr": [
            "timeline_kafka_consumer_skips_total",
            "read_kafka_consumer_skips_total"
          ],
          "alias": [
            "timeline-service / {{reason}} / {{consumer}}",
            "read-service / {{reason}} / {{consumer}}"
          ]
        }
      ]
    }
  ]
}
